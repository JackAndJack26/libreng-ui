export const loader = {
    mask: {
        background: '{content.background}',
        color: '{text.muted.color}'
    },
    icon: {
        size: '2rem'
    }
};

export const css = /*css*/ ``;

export default {
    loader,
    css
};
